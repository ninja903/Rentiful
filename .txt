import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
};  

providers
"use client";

import { ClerkProvider, SignedIn, SignedOut, RedirectToSignIn } from "@clerk/nextjs";
import StoreProvider from "@/state/redux";

const Providers = ({ children }: { children: React.ReactNode }) => {
  return (
    <StoreProvider>
      <ClerkProvider>
        <Auth>{children}</Auth>
      </ClerkProvider>
    </StoreProvider>
  );
};

export default Providers;

const Auth = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className="h-full">
      <SignedIn>
        {children}
      </SignedIn>
      <SignedOut>
        <RedirectToSignIn />
      </SignedOut>
    </div>
  );
};

[
  {
    "applicationDate": "2024-01-01T00:00:00.000Z",
    "status": "PENDING",
    "propertyId": 1,
    "tenantClerkId": "tenant_clerk_001",
    "name": "Jane Tenant",
    "email": "jane.tenant@example.com",
    "phoneNumber": "555-111-2222",
    "message": "Looking forward to hearing from you!"
  },
  {
    "applicationDate": "2024-01-05T00:00:00.000Z",
    "status": "APPROVED",
    "propertyId": 2,
    "tenantClerkId": "tenant_clerk_002",
    "name": "Mark Tenant",
    "email": "mark.tenant@example.com",
    "phoneNumber": "555-333-4444",
    "message": "Very interested in this property."
  }
    {
    "id": 15,
    "applicationDate": "2023-08-05T00:00:00Z",
    "status": "Pending",
    "propertyId": 4,
    "tenantClerkId": "user_15_olivia",
    "name": "Olivia Chen",
    "email": "olivia.chen@example.com",
    "phoneNumber": "+1 (555) 159-7531",
    "message": "I'm looking for a spacious family home like this one."
  }
]
